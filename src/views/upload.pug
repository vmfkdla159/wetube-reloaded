extends layout.pug

block content 
    form(method="post")
        input(type="text", name="upload-title", placeholder="Enter your Video title", required)
        input(type="submit", value="Upload Video") 